## 目标
- 结合 README 与开发日志，精准梳理当前项目状态与能力边界。
- 在“功能完整（Feature Complete）”基础上，制定更聚焦、更务实的下一步行动计划。

## 现状摘要（来源）
- README: 项目功能完备，核心模块覆盖评估、雷达图、历史、AI、分享（/d:/code/y12_ultimatewheel/UW-qoder/README.md:36–40）。
- 开发日志: 已完成 16/16 阶段，包含历史增强（对比/趋势）、分享功能、AI 智能分析与首页模块迁移（/d:/code/y12_ultimatewheel/UW-qoder/log.md:753–771, 522–559, 562–661, 713–748）。
- 当前结论：功能完整、体验优秀、架构清晰；无后端，AI 采用“用户自带 Key + SiliconFlow”模式。

## 执行计划（更新版）
### 阶段一：运行验证与体验检查（巩固 Feature Complete）
1. 本地运行与关键流程回归：快速评估/深度评估/目标设定/历史对比/趋势/分享/AI 分析（含首页迁移模块）。
2. Web 部署验证：GitHub Pages 基础路径（base-href）、Netlify/Vercel SPA 路由与缓存头一致性。
3. 观察日志与异常：`lib/main.dart:16–41`、`lib/services/logger_service.dart:14–37`，确认生产环境日志级别策略。

### 阶段二：测试与覆盖率（质量提升）
1. 引入覆盖率采集与报告：`coverage`/`lcov`，CI 输出覆盖率。
2. 关键集成测试：
   - AI 流式解析与缓存（`EnhancedAiService._generateReportContent` 与 `AiReportStorageService`）。
   - 存储层 CRUD（`StorageService`）。
   - UI 可视化金丝雀测试（雷达图组件）。
3. 完成后在 CI 生成并发布覆盖率报告。

### 阶段三：文档与部署一致性（信息公开与可维护性）
1. README 增补运行/构建指南（Flutter 版本、`flutter run -d chrome`、`flutter build web --release`、GitHub Pages `--base-href`）。
2. 部署配置统一说明：Netlify/Vercel/GitHub Pages 的 SPA 路由重写与 HTTP 头。
3. 设置页说明：`AI 设置`区的 Key 输入与校验流程，提升可用性。

### 阶段四：数据导出/导入（对齐日志“下一步”）
1. 评估数据 JSON 导出/导入能力，含数据结构校验与错误处理。
2. 设置页入口与操作流（备份/恢复）。

### 阶段五：AI 功能增强（对齐日志“下一步”）
1. 多模型支持与模型选择 UI。
2. AI 报告历史记录与对比能力（版本化与标签管理）。
3. 健壮性优化：超时/重试策略与失败回退提示（保留现有流式结构，完善异常兜底）。

### 阶段六：安全与可观测性
1. 生产环境日志级别下调（warning），避免过度输出。
2. 接入错误上报（如 Sentry）与性能采样。
3. API Key 校验与存储加固（最小暴露原则）。

## 交付物
- 本地与 Web 运行验证记录与问题清单。
- 覆盖率报告与 CI 输出。
- 更新后的运行/部署文档与设置说明。
- 数据导出/导入与扩展的 AI 功能原型或实现说明。

## 成功标准
- 核心流程稳定可用，Web 部署一致、无路由/缓存异常。
- 测试通过且覆盖率可视化。
- 文档清晰，AI/数据功能增强落地或形成可评估原型。
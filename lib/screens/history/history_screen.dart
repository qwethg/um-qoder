import 'package:flutter/material.dart';

/// 历史记录页 (04)
class HistoryScreen extends StatelessWidget {
  const HistoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('历史记录'),
      ),
      body: const Center(
        child: Text('历史记录页面 - 开发中'),
      ),
    );
  }
}

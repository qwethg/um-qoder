import 'package:flutter/material.dart';

/// 快速评估页 (03-3)
class QuickAssessmentScreen extends StatelessWidget {
  const QuickAssessmentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('快速评估'),
      ),
      body: const Center(
        child: Text('快速评估页面 - 开发中'),
      ),
    );
  }
}
